# Test-Subnet-Boundaries-in-Packet-Tracer
This project demonstrates how to calculate subnet boundaries, assign correct IP addresses, and verify host-to-host communication inside Cisco Packet Tracer.
Router(config)# interface g0/0
Router(config-if)# ip address X.X.X.X Y.Y.Y.Y
Router(config-if)# no shutdown
ping <destination IP>
